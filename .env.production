# Loaded automatically by `vite build` (production). Docker builds ignore `.env`, so this file
# makes VITE_* available in CI/Dokploy unless you override with a build-arg.
VITE_API_BASE_URL=https://api.job-platform.hugomalzieux.com
